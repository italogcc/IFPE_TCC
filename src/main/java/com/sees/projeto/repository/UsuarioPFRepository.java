package com.sees.projeto.repository;

import com.sees.projeto.entity.Usuario_PF;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UsuarioPFRepository extends JpaRepository<Usuario_PF, Long> {
}

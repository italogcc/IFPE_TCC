package com.sees.projeto.repository;

import com.sees.projeto.entity.Usuario_A;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UsuarioARepository extends JpaRepository<Usuario_A, Long> {
}
